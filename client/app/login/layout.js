export const metadata = {
  title: 'Login | CafeOS',
  description: 'Access your CafeOS dashboard to manage orders, staff, and inventory across all branches.',
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
