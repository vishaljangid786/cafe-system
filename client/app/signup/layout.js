export const metadata = {
  title: 'Sign Up | CafeOS',
  description: 'Join the CafeOS platform. Register your profile to manage cafe operations, staff, and locations.',
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
